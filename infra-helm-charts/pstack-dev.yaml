alertmanager:
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      external-dns.alpha.kubernetes.io/hostname: alertmanager-dev.poornachandra3.online
    hosts:
      - alertmanager-dev.poornachandra3.online

grafana:
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      external-dns.alpha.kubernetes.io/hostname: grafana-dev.poornachandra3.online
    hosts:
      - grafana-dev.poornachandra3.online

prometheus:
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      external-dns.alpha.kubernetes.io/hostname: prometheus-dev.poornachandra3.online
    hosts:
      - prometheus-dev.poornachandra3.online